{
  "name": "@cardinal/cli",
  "version": "0.0.0",
  "type": "module",
  "bin": { "cardinal": "./dist/bin.js" },
  "scripts": {
    "test": "vitest run",
    "build": "tsup src/bin.ts --format esm",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cardinal/core": "workspace:*",
    "fast-glob": "^3.3.0"
  }
}
